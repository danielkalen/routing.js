{
  "name": "routing.js",
  "version": "1.0.3",
  "description": "Simple, lightweight, promise-based client-side routing supporting IE8+",
  "main": "dist/routing.js",
  "browser": {
    "./debug": "dist/routing.debug.js",
    "./src": "src/routing.coffee"
  },
  "scripts": {
    "postversion": "echo \\'$(json -f package.json version)\\' > .config/.version.coffee && npm run build",
    "test": "npm run test:electron",
    "test:local": "open test/testrunner.html",
    "test:karma": "karma start .config/karma.conf.coffee",
    "test:electron": "karma start --single-run --browsers Electron .config/karma.conf.coffee",
    "test:chrome": "karma start --single-run --browsers Chrome .config/karma.conf.coffee",
    "test:safari": "karma start --single-run --browsers Safari .config/karma.conf.coffee",
    "test:sauce": "coffee .config/sauce-launch.coffee && npm run test-serve",
    "test-serve": "http-server -p 9201 --cors ./",
    "sauce-connect": "sc -u routingjs -k 9321bba9-2293-4e66-8fb5-06e255fbaa51",
    "build": "npm run compile && npm run minify",
    "compile": "npm run compile:js && npm run compile:test",
    "compile:js": "simplyimport -i src/routing.coffee | coffee -cbs --no-header > dist/routing.debug.js",
    "compile:test": "simplyimport -i test/test.coffee | coffee -cbs > test/test.js",
    "minify": "npm run minify:super && npm run minify:manual && npm run minify:simple",
    "minify:simple": "uglifyjs dist/routing.debug.js -m -c keep_fargs,unused=false -o dist/routing.js",
    "minify:super": "closure-service dist/routing.debug.js > dist/routing.js",
    "minify:manual": "coffee .config/manual-minify.coffee",
    "watch": "npm run watch:js & npm run watch:test",
    "watch:js": "simplywatch -g 'src/*.coffee' -x 'npm run compile:js -s'",
    "watch:test": "simplywatch -g 'test/test.coffee' -x 'npm run compile:test -s'",
    "coverage:badge": "badge-gen -d ./.config/badges/coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danielkalen/routing.js.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/danielkalen/routing.js/issues"
  },
  "homepage": "https://github.com/danielkalen/routing.js#readme",
  "devDependencies": {
    "badge-gen": "^1.0.1",
    "bluebird": "^3.5.0",
    "chai": "^3.5.0",
    "chai-spies": "^0.7.1",
    "closure-compiler-service": "^0.6.1",
    "coffee-script": "^1.12.4",
    "foreach-cli": "^1.7.1",
    "jquery": "^3.2.1",
    "json": "^9.0.6",
    "karma": "^1.5.0",
    "karma-chai": "^0.1.0",
    "karma-chrome-launcher": "^2.0.0",
    "karma-coverage": "^1.1.1",
    "karma-electron-launcher": "^0.2.0",
    "karma-firefox-launcher": "^1.0.1",
    "karma-mocha": "^1.3.0",
    "karma-mocha-reporter": "^2.2.3",
    "karma-opera-launcher": "^1.0.0",
    "karma-safari-launcher": "^1.0.0",
    "mocha": "^3.2.0",
    "simplyimport": "^3.3.5",
    "electron": "^1.6.2",
    "simplywatch": "^2.5.0"
  },
  "dependencies": {
    "smart-extend": "^1.3.2"
  }
}
